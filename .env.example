# chexian-query 配置文件示例
# 复制本文件为 .env(项目根),再按需修改
#   cp .env.example .env

# 数据根目录(指向 parquet 所在的位置)
#
# 选项 A:本地数据(默认值,无需配置)
# DATA_BASE=./data
#
# 选项 B:Windows SMB 网络共享(推荐,跨同事共享一份数据)
# 注意路径里的反斜杠会被自动转换,正反斜杠都支持
# DATA_BASE=//10.120.0.87/chexian-query/data
#
# 选项 C:Mac 挂载的 SMB 共享(数据负责人 Mac 端用)
# DATA_BASE=/Volumes/chexian-query/data
#
# 选项 D:绝对路径(本地或挂载盘)
# DATA_BASE=D:/datasets/chexian
